import React from 'react';
import { render, screen } from '@testing-library/react';
import CardComponent from './CardComponent';

test('render', () => {
    render(<CardComponent title="Life Product" text="hello customer" imgcomponent="" />)
    expect()
})